pages:
  build-job:
    stage: build
    script:
      - echo "Hello, $GITLAB_USER_LOGIN!"

  test1:
    stage: test
    script:
      - echo "This job tests something"

  done:
    stage: deploy
    script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
    artifacts:
      paths:
      - public
    only:
    - main
